#!/usr/bin/env doomscript

(cd (getenv "DOOMDIR"))

(defcli! buttercup ()
         (require 'doom-start)
         (require 'buttercup)
         ;; prepend . to load path
         (setq load-path (cons "." load-path))
         (buttercup-run-discover))

(run! "buttercup")

