cd ~/.local/share/chezmoi/private_dot_config/exact_nixpkgs
niv update && (chezmoi apply ~/.config/nixpkgs && git add nix && git commit -m "niv update" && git push) || git checkout -- nix/sources.json
